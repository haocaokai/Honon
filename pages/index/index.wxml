<!--index.wxml-->
<view class="container flex-wrap flex-direction-col">
  <view class="my-swiper">
      <swiper indicator-dots="{{indicatorDots}}"
    autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
    <block wx:for="{{imgUrls}}">
      <swiper-item>
        <image src="{{item}}" class="slide-image" height="150"/>
      </swiper-item>
    </block>
  </swiper>
  </view>

    <!-- 分类导航 -->
  <!-- <view class="nav-block wrap">
    <block wx:for="{{navItems}}"> 
      <view class="wrap-item {{item.isSplot ? 'exp' : ''}}">  
        <navigator url="../components/{{item.url}}/{{item.url}}" hover-class="navigator-hover">{{item.name}}</navigator>
      </view>
    </block>
  </view> -->
  <view class="wrap">
    <!-- <view>{{title}}</view>
    <view class="shop" wx:for="{{shops}}" wx:key="id">
      <navigator url="/pages/shop/shop?id={{item.id}}">
        <image src="{{item.img}}"/>
      </navigator>
		</view> -->
    

    <view class="contact-wrapper">
      <view>联系人：孙忠</view>
      <view>联系电话：13901203458 / 13522080638</view>
      <view bindtap="openMap">地址：北京市朝阳区北五环建材灯饰城4层D178</view>
    </view>


    <button type="primary" bindtap="call" class="btn call">电话联系</button>  
    <button type="primary" bindtap="openMap" class="btn openMap">导航到店</button>

   
  </view>
</view>